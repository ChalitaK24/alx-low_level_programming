
#include <stdio.h>

/**
 * main - lowercase uppercase then new line
 *
 * Return: 0 Success
 */

int main(void)
{
	int i;
		{
		for (i = 'a'; i <= 'z'; i++)
			putchar (i);
		for (i = 'A'; i <= 'Z'; i++)
			putchar (i);
		}
		putchar ('\n');
	return (0);
}
